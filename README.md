# Autodesk-Uninstaller
Will uninstall and delete all data associated with Autodesk in %ProgramFiles%, %ProgramFiles(x86)%, %ProgramData%, HKey Users/LocalMachine/CU, and %LocalAPPDATA% for every user on the local machine
